<h1>トップページ</h1>
<%=link_to '新規投稿', '/posts/new'%>
<% @posts.each do |post|%>   <%#の後ろに=がないのは、表示する必要がないから.%>
<div class="post">
    <div class="post-date">
      投稿日時：<%= post.created_at %>
    </div>
    <div class="post-content">
      <%= post.content %>
    </div>
  </div>
<% end %>
  <%# ↑↓ここにrubyでのコードを入れると実装(表示）されるよ！ %>
  <%# <%= @like %> 